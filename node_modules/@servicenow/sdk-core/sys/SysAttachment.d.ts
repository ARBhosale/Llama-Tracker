import { Entity } from '../entity';
import { Record } from '../db/Record';
export type SysAttachmentConfig = {
    id: string;
    fileName: string;
    contentType: string;
    filePath: string;
    tableName: string;
    tableSysId: string;
};
export declare function SysAttachment(config: SysAttachmentConfig): SysAttachmentConfig & {
    readonly asRecord: () => Entity<Record<{
        readonly name: "sys_attachment";
    }>, import("../db").Table>;
};

import type { Column, Typed } from '../Column';
export declare function BooleanColumn<const Type extends boolean, const Default extends Type, const P extends Column<Type, Default>>(properties: P & Column<Type, Default>): Typed<Type, P> & {
    readonly asRecord: () => never;
};

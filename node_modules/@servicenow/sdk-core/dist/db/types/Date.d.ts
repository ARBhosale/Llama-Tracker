import type { Column, Typed } from '../Column';
export declare function DateColumn<const Type extends Date, const Default extends Type, const P extends Column<Type, Default>>(properties: P & Column<Type, Default>): Typed<Type, P> & {
    readonly asRecord: () => never;
};

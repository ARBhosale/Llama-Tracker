import type { Column, Typed } from '../Column';
import type { Record } from '../Record';
import type { Table } from '../Table';
type ReferenceColumn<RefTable extends Table = Table, Type extends Record<RefTable> | string = Record<RefTable> | string, // Allowing strings to be assigned is an escape hatch
Default extends Type = Type> = Column<Type, Default> & {
    referenceTable?: RefTable;
};
export declare function ReferenceColumn<const RefTable extends Table, const Type extends Record<RefTable> | string, const Default extends Type, const P extends ReferenceColumn<RefTable, Type, Default>>(properties: P & ReferenceColumn<RefTable, Type, Default>): Typed<Type, P> & {
    readonly asRecord: () => never;
};
export {};

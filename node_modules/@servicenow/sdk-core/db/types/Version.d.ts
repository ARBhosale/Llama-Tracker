import { Entity } from '../../entity';
import type { Column, Typed } from '../Column';
export declare function VersionColumn<const Type extends `${bigint}.${bigint}.${bigint}`, const Default extends Type, const P extends Column<Type, Default>>(properties: P & Column<Type, Default>): Typed<Type, P> & {
    readonly asRecord: () => Entity<import("..").Record<import("..").Table>, import("..").Table>;
};

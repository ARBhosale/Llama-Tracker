import { Entity } from '../../entity';
import type { Column, Typed } from '../Column';
import type { Record } from '../Record';
import type { domain } from '../../tables';
export declare function DomainIdColumn<const Type extends Record<domain> | 'global', const Default extends Type, const P extends Column<Type, Default>>(properties: P & Column<Type, Default>): Typed<Type, P> & {
    readonly asRecord: () => Entity<Record<import("..").Table>, import("..").Table>;
};
